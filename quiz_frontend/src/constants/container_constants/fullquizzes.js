import { createRequestTypes } from './../../helpers';

export const GET_QUIZZES_BY_TYPE = createRequestTypes('GET_QUIZZES_BY_TYPE');
export const SET_FULL_QUIZZES_ACTIVE_KEY = 'SET_FULL_QUIZZES_ACTIVE_KEY';
